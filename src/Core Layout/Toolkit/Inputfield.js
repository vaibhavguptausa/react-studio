import React, { Component } from 'react';

export default class InputField extends Component {
  render() {
    return <div style={{height: `${100}px` , width: `${100}px`,}}><input placeholder='write something'/></div>
  }
}